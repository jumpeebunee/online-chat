export type IUser = {
  email: string | null,
  accessToken: string | null,
  id: string | null,
}

export type IUserData = {
  firstName: string,
  lastName: string, 
  email: string, 
  password: string
}